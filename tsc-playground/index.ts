// string
const string = "foo"

// boolean
const boolean = false

// enum
enum Enum {
  one = 1,
  two = 2,
  three = 3
}

// function
const fooFunction = (array: string[]): void => {
  array.forEach((message) => console.log(message))
}
